<%= form_with(model: topic) do |form| %>
  <% if topic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
        <% topic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :title, style: "display: block", class: "form-label" %>
    <div class="col-sm-5"><%= form.text_field :title , class: "form-control"%></div>
  </div>

  <div class="mb-3">
    <%= form.label :content, style: "display: block", class: "form-label" %>
    <div class="col-sm-5"><%= form.text_area :content, class: "form-control" %></div>
  </div>

  <div class="actions">
    <%= form.submit  class: "btn btn-success" %>
  </div>
<% end %>
